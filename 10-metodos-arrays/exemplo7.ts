// slice -> retorn uma cópia contendo a parte de uma array de acordo com os indices informados

const paises = ['Brasil', 'França', 'Argentina', 'Italia', 'Alemanha']; 

console.log(paises.slice(1, 4));